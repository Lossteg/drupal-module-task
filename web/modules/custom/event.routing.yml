event.event_page:
  path: '/events/{node}'
  defaults:
    _controller: '\Drupal\event\Controller\EventRegisterController::build'
  requirements:
    _entity_access: 'node.view'
    node: '\d+'
  options:
    parameters:
      node:
        type: entity:node
        bundle:
          - event
